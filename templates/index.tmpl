<!DOCTYPE html>
<html>
    {{template "header" . }}
    <body>
        <main class="wrapper">
            {{template "navigation"}}
            <section class="container">
                <p>Hi, your random quote is the following:</p>
                <pre><code>{{ .quote }}</code></pre>
                <p>
                    <a class="button" href="/">Next Please</a>
                    <a class="button button-outline" href="/add">Add New Quote</a>
                    <a class="button button-outline" href="/manage">Manage Quotes</a>
                </p>
            </section>
        </main>
    </body>
</html>
